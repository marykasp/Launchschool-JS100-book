// iterating over a list of names using a for loop

let names = ['Chris', 'Kevin', 'Naveed', 'Pete', 'Victor'];
let upperNames = [];

// initialization variable, condition, increment
for (let index = 0; index < names.length; i++) {
  let upperCaseNames = names[index].toUpperCase();
  upperNames.push(upperCaseNames);
}

console.log(names);
console.log(upperNames)
